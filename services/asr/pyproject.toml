[project]
name = "echoid-asr"
version = "0.1.0"
description = "EchoID ASR microservice (faster-whisper + FastAPI)"
requires-python = ">=3.11,<3.13"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "python-multipart>=0.0.12",
    "faster-whisper>=1.0.3",
]
