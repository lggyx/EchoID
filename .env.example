# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# DB
DATABASE_URL="file:./dev.db"

# Storage
STORAGE_DIR=./storage
AUDIO_TTL_HOURS=24

# Providers: mock | fasterwhisper (mvp defaults to fasterwhisper)
ASR_PROVIDER=fasterwhisper
ASR_ENDPOINT=http://192.168.64.1:8000
ASR_LANGUAGE=zh
LLM_PROVIDER=mock
IMAGE_PROVIDER=mock
